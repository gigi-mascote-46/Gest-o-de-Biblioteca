🔌 Interfaces encontradas no teu projeto

1. IUnidadeDeTrabalho

Padrão usado: Unit of Work

Função: Gerir transações de base de dados. Garante que todas as alterações em repositórios sejam tratadas como uma única operação atómica.

Usos comuns:

IniciarTransacao()

Confirmar()

Cancelar()

💡 Evita que operações parciais fiquem gravadas em caso de erro.
---------------
2. IRepositorioEmprestimos
3. IRepositorioExemplares
4. IRepositorioLivros
5. IRepositorioUsuarios

Estas quatro seguem o Padrão Repositório, e têm funções semelhantes, aplicadas a diferentes entidades:

📚 Função comum dos Repositórios
Isolam o acesso à base de dados

Fornecem operações CRUD (Create, Read, Update, Delete) para cada tipo de entidade

Facilitam testes e manutenção, ao desacoplar a lógica de negócio da tecnologia de persistência

-----------------

🧠 Por que estas interfaces são importantes no teu projeto?

🔄 Flexibilidade: podes mudar a base de dados (ex: de SQLite para SQL Server) sem alterar a lógica da aplicação.

🧪 Testabilidade: facilita o uso de mocks e testes unitários.

💼 Organização: separa claramente responsabilidades entre camadas da aplicação.

